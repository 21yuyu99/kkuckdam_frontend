import { configureStore, createSlice } from '@reduxjs/toolkit';
const initialState = {
  name: '아이유', // state 초기값
};

const personSlice = createSlice({
  name: 'person',
  initialState,
  reducers: {
    
  },
});